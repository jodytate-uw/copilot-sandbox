# September 4, 2026 - 04:18pm - Claude Opus 5

## Work Summary

Reviewed the Copilot interaction history for this repository from late August 2026 to the present and produced a set of prompt-engineering recommendations for later review.

## Tasks Completed

**History review**
- Queried the local session store for all sessions created on or after 2026-08-01: six sessions, roughly 90 turns, five in `copilot-sandbox` and one in `uw-oris/eras-budget`
- Pulled the user-message text from the two longest sessions (57 and 6 turns) to identify recurring patterns
- Cross-referenced the two existing session logs in `sessions/` and the current conversation

**Recommendations document**
- Created `sessions/prompt-engineering-recommendations.md` per `user_prompts/prompt_09042026_02.md`
- 13 bullets, roughly 950 words, each pairing a recommendation with its implication
- Closing summary reduces the list to three moves: persist what recurs, be explicit about scope and intent, separate research from build phases

## Findings

The dominant source of lost time was unstated context rather than assistant capability:

- Conventions defined verbally and recorded only in session prose, then missed later (the commit trailers on `a61c861` and `ce077fa`)
- Ambiguity between questions and instructions, which led to unrequested implementation work
- Unscoped git commands — `git commit and push` interpreted as `git add -A`, sweeping a directory deletion and unrelated code into one commit
- Environment prerequisites discovered mid-task: Node.js PATH, PowerShell execution policy, missing GitHub CLI
- Naming and format churn (`diary_entries` → `log` → `sessions`; three iterations on the timestamp format)

The `user_prompts/` file-based prompt pattern was identified as the user's most effective existing practice.

## Decisions

- Placed the recommendations in `sessions/` rather than the repository root, since it is a review artifact rather than project documentation
- Left the stash and prior commit history untouched, per earlier instruction

## Next Steps

- Review the recommendations and decide which to encode in `.github/copilot-instructions.md`
- `stash@{0}` remains parked with the unrequested CI workflow; no action taken

## Notes

Session-store queries rejected the `replace()` function as a blocked statement; `substr()` alone was sufficient for extracting prompt text.
